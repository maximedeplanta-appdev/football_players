<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All midfielders
    </h1>

    <a href="/midfielders/new" class="btn btn-block btn-outline-secondary">
      Add a new midfielder
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Position
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @midfielders.each do |midfielder| %>
      <tr>
        <!-- Display midfielder.id start -->
        <td>
          <%= midfielder.id %>
        </td>
        <!-- Display midfielder.id end -->

        <!-- Display midfielder.position_id start -->
        <td>
          <% if midfielder.position.present? %>
            <a href="/positions/<%= midfielder.position.id %>">
              <%= midfielder.position.created_at %>
            </a>
          <% end %>
        </td>
        <!-- Display midfielder.position_id end -->

        <!-- Display midfielder.created_at start -->
        <td>
          <%= time_ago_in_words(midfielder.created_at) %> ago
        </td>
        <!-- Display midfielder.created_at end -->

        <!-- Display midfielder.updated_at start -->
        <td>
          <%= time_ago_in_words(midfielder.updated_at) %> ago
        </td>
        <!-- Display midfielder.updated_at end -->

        <td>
          <a href="/midfielders/<%= midfielder.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @midfielders, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
